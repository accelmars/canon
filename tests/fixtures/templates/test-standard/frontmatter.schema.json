{
  "required": ["type"],
  "properties": {
    "title": { "type": "string" },
    "type": {
      "type": "string",
      "enum": ["identity", "reference", "index"]
    },
    "engine": { "type": "string" },
    "status": {
      "type": "string",
      "enum": ["active", "draft", "archived"]
    },
    "schema_version": { "type": "integer" },
    "created": { "type": "string" },
    "updated": { "type": "string" },
    "category": { "type": "string" },
    "boundary": { "type": "string" }
  }
}
